package(default_visibility = ["//visibility:public"])

load("@angular//:index.bzl", "ng_module")

ng_module(
    name = "shared",
    srcs = ["index.ts"],
    tsconfig = "//:tsconfig.json",
    deps = [
        "@rxjs",
    ]
)
